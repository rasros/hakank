output let {array [int] of string: X_INTRODUCED_41_ = (([((("k: "++show(k))++"\n")++show(x))++"\n"]++["maximal independent set: {"])++[if fix(x[i])==1 then show(i)++" " else "" endif | i in 1..n])++["}"]} in (X_INTRODUCED_41_);
int: n = 8;
array [1..n] of int: x;
int: k = sum(x);
